# ضمان الجودة

## 1. الفحص الآلي

```bash
npm run verify
```

يشغّل فحص Manifest وCSP والأذونات والملفات والخطوط، ثم 72 اختبارًا تغطي:

| الملف | ما يحرسه |
|---|---|
| `tests/settings.test.mjs` | عقد الإعدادات، تطبيع النطاقات، أسبقية قواعد المواقع، المظهر. |
| `tests/background.test.mjs` | تخزين Service Worker، الترحيل، الشارة لكل تبويب، سلوك الاختصارات على موقع تحكمه قاعدة، بوابة الإيقاف العام. |
| `tests/options.test.mjs` | دورة تحرير المواقع، منع «التخصيص الشبح»، قبول كل نطاق يقبله العقد المشترك. |
| `tests/contract.test.mjs` | تطابق نسخة العقد داخل `content.js` مع `src/shared/*` (بما فيها نمط التحقق من النطاقات)، وعدم وجود رسائل ميّتة. |
| `tests/design-system.test.mjs` | طبقة الرموز الواحدة، أرضية 11px، غياب الألوان الخام، تطابق كتلتي المظهر الداكن. |
| `tests/ui-separation.test.mjs` | فصل مظهر الأداة عن أوضاع الصفحة، صدق اللوحة ونطاق تحريرها، حصر الحجم والتباعد في النثر، وحدة المقاس، التحديث في المكان بدل إعادة البناء، متابعة تغيّر المقاس، ترويسة المقال، وجذور الظل. |

## 2. الفحص في المتصفح

بعض السلوك لا يمكن اختباره دون DOM حقيقي، والمشروع بلا تبعيات. لذلك توجد بيئة تشغيل محلية
تُحمّل الكود المشحون نفسه مع محاكاة `chrome.*`:

```bash
npm run harness
```

| العنوان | يغطّي |
|---|---|
| `/tests/harness/popup.html` | اللوحة كاملة. المعاملات: `?theme=dark`, `?page=protected`, `?page=reload`, `?host=…`, `?readingRoot=false` |
| `/tests/harness/options.html` | صفحة إدارة المواقع كاملة. |
| `/tests/fixtures/content-harness.html` | المحرك مع محاكاة `chrome.*` مضمّنة في الملف نفسه — أسرع طريقة لفتح المحرك بلا معاملات. |
| `/tests/fixtures/engine-regressions.html` | تراجعات المحرك في 1.2: تبديل `class`، إعادة كتابة `style`، رشقة التخزين. |
| `/tests/fixtures/real-article.html?mode=reading` | مقال عربي/إنجليزي بخط مصمم واتجاه صريح وكود وأزرار. |
| `/tests/fixtures/github-like-grid.html?mode=reading` | تطبيق شبكي مع شريط جانبي ومقال داخلي. |
| `/tests/fixtures/app-no-article.html?mode=reading` | تطبيق ديناميكي بلا مقال موثوق. |

### تشغيل المحرك على صفحة حقيقية

سياسات CSP تمنع حقن السكربت في المواقع مباشرة، لذا تُلتقط الصفحة ثم تُشغَّل محليًا:

```bash
curl -sL -A "Mozilla/5.0 … Chrome/131" "https://github.com/<user>" -o /tmp/page.html
```

ثم استبدل الروابط النسبية بمطلقة، احذف سكربتات الصفحة و`<meta http-equiv="Content-Security-Policy">`،
وأضف قبل `</body>`:

```html
<script src="/tests/harness/page-mock.js"></script>
<script src="/src/content/content.js"></script>
```

ضع الملف داخل المستودع مؤقتًا وافتحه عبر `npm run harness`. لا تُضِف اللقطات إلى المستودع.

تقبل صفحات `fixtures/` أي مفتاح من عقد الإعدادات كمعامل استعلام، مثل
`?mode=reading&fontSize=18&textWidth=88&reduceVisualNoise=true`.

## 3. مصفوفة إصدار 1.3.0

قياسات فعلية على صفحات حقيقية داخل متصفح، على الكود المشحون:

| الموقع | الخط المُعلَن | قبل: متخطّى | بعد: متخطّى | يُعاد تنسيقه | يتغير حجمه (نثر) | خط فقط (واجهة) |
|---|---|---|---|---|---|---|
| GitHub (ملف شخصي) | `Mona Sans VF` | **100%** | 2% (`Mona Sans Mono`) | **98%** | 48 | 577 |
| YouTube | `Roboto` | 98% | **0%** | **100%** | 0 | 56 |
| Substack | `Spectral` | 16% | **0%** | **100%** | 13 | 56 |
| ويكيبيديا | `sans-serif` | 1% | 0% | 100% | 221 | — |

| السيناريو | النتيجة المطلوبة | النتيجة |
|---|---|---|
| GitHub وضع تصميم | خط جديد بلا أي إزاحة | نجح؛ `main`/`header` = 1265px قبل وبعد، **صفر رابط أو زر تغيّر حجمه** |
| GitHub وضع قراءة | لا يُعامَل كمقال | نجح؛ رُفض بـ«نثر 10% < 45%»، لا جذر ولا CSS |
| ويكيبيديا وضع قراءة | مقال حقيقي يُقبل ويضيق دون كسر | نجح؛ الجذر `max-width: none` وعرضه 957px، والفقرات 792px |
| حماية خطوط الأيقونات | تبقى محميّة | نجح؛ `Material Icons` و`ui-monospace` مرفوضان |
| Popup: تسع حالات متتالية | ثبات العرض | نجح؛ **358/380 ثابتة** في كل الحالات |
| إدارة المواقع: تعديل بلا حفظ | لا يصل التخزين | نجح؛ التخزين دون تغيير و«معاينة — لم تُحفظ بعد» |
| إدارة المواقع: تجاهل | يعود للقيمة المحفوظة | نجح |
| إدارة المواقع: حفظ | يُكتب التخصيص | نجح |

### جولة 1.2.0 (ما زالت سارية باستثناء صف واحد)

> صف «خط Georgia مصمم → لا يُستبدل» **لم يعد ساريًا** منذ 1.3.0: صار الحارس يحمي خطوط
> الأيقونات وأحادية العرض فقط، فالخط المصمَّم يُستبدل عمدًا. انظر بند 4 في
> [ARCHITECTURE.md](ARCHITECTURE.md).


| السيناريو | النتيجة المطلوبة | النتيجة |
|---|---|---|
| تطبيق GitHub-like في وضع القراءة | ثبات أعمدة التطبيق والشريط الجانبي والألوان | نجح؛ بقيت الأعمدة `280px 1000px` والشريط `280px` |
| المقال داخل التطبيق | تضييق المقال وحده | نجح؛ `max-width: 560.6px`، و4 عناصر معلَّمة داخله و0 خارجه |
| مقال حقيقي في وضع القراءة | حجم أساسي دقيق ونِسب عناوين محفوظة | نجح؛ النص 18px والعنوان 42.75px، وألوان الصفحة كما هي |
| اتجاه `dir="ltr"` صريح | لا يُعدَّل ولا يُحذف | نجح |
| تطبيق بلا مقال | لا style ولا root ولا markers | نجح؛ 200 تغيير سمة متتالٍ دون أي إعادة تطبيق |
| **تبديل `class` على `<body>`** | لا يومض النص ولا تُمسح العلامات | نجح؛ بقي العدد 4 قبل وأثناء وبعد ست دورات |
| **إعادة كتابة `style` كاملة** | يستعيد العنصر حجمه فورًا | نجح؛ عاد `16.000px` وبقيت العلامة |
| **رشقة 14 كتابة تخزين** | إعادة تطبيق واحدة | نجح؛ `14 → 1` |
| Popup: مظهر نظام/نهاري/ليلي | دورة صحيحة وحفظ | نجح |
| Popup: إعادة ضغط الوضع النشط | لا تمسح الضبط اليدوي | نجح؛ بقي `lineHeight: 2` |
| Popup: تحرير نطاق فرعي | ترث القاعدة الجديدة ما تحجبه | نجح؛ نُسخ `arabicFont` و`fontSize` من قاعدة الأب |
| Popup: وضع قراءة بلا مقال | تفسير صريح بدل ادعاء التفعيل | نجح؛ «لا يوجد مقال موثوق في هذه الصفحة…» |
| Options: موقع مستثنى فقط | قابل للفتح في المحرر | نجح |
| Options: `localhost` | يُقبل ويبقى بعد الحفظ | نجح |
| Options: موقع بلا تخصيص | لا يُكتب له شيء | نجح؛ لا مدخل في `snfont.siteSettings` |
| Options: سحب شريط الحجم | لا يعيد بناء القائمة | نجح |

## 4. مصفوفة إصدار 1.4.0

قياسات فعلية في Chromium محمَّلة فيه الإضافة عبر Playwright، على الكود المشحون:

| السيناريو | النتيجة المطلوبة | النتيجة |
|---|---|---|
| موقع تحكمه قاعدة: قيم اللوحة | القيم الفعّالة لا العامة | نجح؛ `20px` و`Cairo` بدل `16px` و`Noto Sans Arabic` |
| موقع تحكمه قاعدة: سحب الشريط | تتغيّر الصفحة فعلًا | نجح؛ الكتابة ذهبت إلى القاعدة (`28`) والعام بقي `16`، والفقرة صارت `28px` |
| موقع بلا قاعدة | تُحرَّر الإعدادات العامة | نجح؛ العام `24` وبلا أي قاعدة جديدة، وبلا شريط تنبيه |
| تغيير إعداد على صفحة 3000 فقرة | لا ترتد الصفحة إلى خطها الأصلي | نجح؛ الأدنى **3006 من 3006** (كان **0**)، والاستقرار 373ms (كان 564ms) |
| `letterSpacing: 0.2` في وضع التصميم | لا تتحرك مقاسات الواجهة | نجح؛ الشارة وخلية الجدول بلا تغيير، و`letter-spacing: normal` عليهما |
| مقال جذره `<main>` | العنوان داخل ترويسة المقال يُنسَّق | نجح؛ العنوان والإسناد معلَّمان، وترويسة الموقع لا |
| مقاس القراءة | عرض واحد لكل كتل النثر | نجح؛ تسع كتل — عنوان واقتباس وفقرات — كلها `792px` |
| نقطة توقّف متجاوبة | يتبع الحجمُ الصفحةَ | نجح؛ العنوان `23.75px` والمتن `20px` بدل التثبيت على القديم |
| Shadow DOM مفتوح | يُنسَّق النص داخله | نجح؛ معلَّم وبحجم `22px` |
| نطاق غير صالح | يُرفض ويُشرح | نجح؛ `not a domain!!` وأخواته تعيد `''`، والقائمة تبقى فارغة |
| الشارة على موقع مستثنى | `OFF` لذلك التبويب وحده | نجح؛ `OFF` للمستثنى و`''` للعادي |
| عنوان الخط في الصفحة | لا يكشف معرّفًا ثابتًا | نجح؛ عنوان الجلسة المتغيّر، والخطوط تُحمَّل |
| إيقاف SnFont | استعادة كاملة شاملة جذور الظل | نجح؛ صفر علامة، صفر ورقة متبنّاة، والمقاسات الأصلية `17px`/`16px` |

## 5. المتصفح الحقيقي عبر Playwright

بيئة `npm run harness` تحاكي `chrome.*`. لقياس ما لا تستطيع محاكاته — الشارة لكل تبويب،
جذور الظل، Service Worker حقيقي، تخزين حقيقي، ومنح `activeTab` — تُحمَّل الإضافة في Chromium
حقيقي:

```bash
npx playwright@latest install chromium
```

ثم شغّل سكربتًا يفتح `chromium.launchPersistentContext` مع
`--disable-extensions-except` و`--load-extension` يشيران إلى جذر المستودع، و
`--host-resolver-rules=MAP * 127.0.0.1:<port>` حتى تصير كل المضيفات مواقع حقيقية على خادم
محلي واحد — وهو ما يسمح باختبار قواعد المواقع على أصول متمايزة فعلًا.

نقطتان تعلّمناهما من هذه البيئة:

- اللوحة تُفتح كتبويب، فلا تكون هي التبويب النشط تلقائيًا. الترتيب الصحيح: افتح اللوحة، ثم
  `bringToFront()` على تبويب الموقع، ثم `reload()` على اللوحة — فتقرأ حينها التبويب الصحيح
  تمامًا كما تفعل في Brave.
- لا يمكن إطلاق `chrome.commands` من Playwright؛ سلوك الاختصارات مغطّى في
  `tests/background.test.mjs`.

## 6. قبل أي إصدار

1. `npm run verify`.
2. كرّر صفوف «تطبيق GitHub-like» و«تطبيق بلا مقال» و«تبديل `class`» عبر بيئة المتصفح أعلاه،
   خصوصًا بعد أي تعديل على `src/content/content.js`.
3. حمّل النسخة غير المضغوطة في Brave الحالي وجرّب **GitHub وYouTube وSubstack وويكيبيديا**
   وصفحة عربية وأخرى مختلطة. تحقّق أن الخط تغيّر وأن التخطيط لم يتحرك.
4. اختبر اللوحة على تبويب `brave://` وعلى تبويب عادي كان مفتوحًا قبل تثبيت الإضافة
   (يجب أن يظهر «أعد تحميل الصفحة»، لا «صفحة محمية»).
5. أعد تشغيل مجموعة Playwright على الأقل لصفوف «موقع تحكمه قاعدة» و«تغيير إعداد على صفحة
   كبيرة» و«Shadow DOM» بعد أي تعديل على `src/content/content.js` أو `src/popup/popup.js`.
6. `npm run package`.

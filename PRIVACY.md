# سياسة خصوصية SnRead

آخر تحديث: 23 أغسطس 2026 — الإصدار 1.4.0

يعالج SnRead النصوص وأنماط الخط داخل جهاز المستخدم فقط بهدف تحسين القراءة. لا يرسل محتوى الصفحات أو سجل التصفح أو الإعدادات إلى خادم، ولا يستخدم تحليلات أو إعلانات أو أدوات تتبع.

تحفظ الإضافة محليًا عبر `chrome.storage.local`:

- إعدادات الخط والقراءة العامة.
- أسماء النطاقات التي خصصها المستخدم أو استثناها.
- الإعدادات الخاصة بكل نطاق.
- اختيار مظهر واجهة SnRead (يتبع النظام أو فاتح أو داكن).

لا تصل الإضافة إلى كلمات المرور أو قيم حقول الإدخال والمحررات، وهذه العناصر مستثناة من محرك المعالجة. تصدير الإعدادات يحدث فقط عندما يضغط المستخدم زر «تصدير»، وينشئ ملف JSON محليًا يختار المستخدم مكان حفظه.

تحتاج الإضافة إلى العمل على صفحات HTTP وHTTPS لأن تحسين القراءة هو وظيفتها الأساسية. لا تعمل على صفحات Brave الداخلية أو صفحات الإضافات أو متجر الإضافات.

الأذونات المطلوبة اثنان فقط: `storage` لحفظ الإعدادات محليًا، و`activeTab` الذي يمنحه
المتصفح لحظة فتح لوحة SnRead أو ضغط أحد اختصاراتها. يُستخدم `activeTab` لقراءة عنوان
التبويب الحالي حصرًا كي تعرف اللوحة إن كانت الصفحة قابلة للتحسين أو محمية، ولا يُخزَّن هذا
العنوان ولا يغادر الجهاز.

## ما تستطيع الصفحة نفسها ملاحظته

لتغيير خط الصفحة يجب أن تُدرَج ورقة أنماط داخلها، وهي ورقة يستطيع أي سكربت في الصفحة قراءتها.
يعني ذلك أن الموقع الذي تزوره يمكنه أن يعرف أن SnRead يعمل، وأن يقرأ الخط والحجم وارتفاع السطر
والتباعد الذي اخترته. لا يغادر أيٌّ من ذلك جهازك إلى SnRead، لكنه ظاهر للموقع.

منذ 1.4.0 تُقدَّم ملفات الخطوط عبر عنوان يتغيّر مع كل جلسة (`use_dynamic_url`)، فلا يستطيع
الموقع استخراج معرّف ثابت للإضافة منها. لإخفاء الأثر كليًا على موقع بعينه، استخدم الاستثناءات.

يمكن حذف جميع البيانات في أي وقت من صفحة «إدارة المواقع» عبر «إعادة الضبط»، أو بإزالة الإضافة من Brave.

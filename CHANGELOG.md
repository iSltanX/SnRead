# سجل التغييرات

## 1.4.0 — 2026-08-23

فحص كامل للإضافة داخل Chromium حقيقي محمَّلة فيه عبر Playwright — لا محاكاة — ثم إصلاح كل
ما ظهر. ستّ عشرة مشكلة، أُعيد إنتاج كلٍّ منها قبل إصلاحها وأُثبت إصلاحها بعده.

### اللوحة كانت تعلن حفظًا لا أثر له

- على موقع تحكمه قاعدة خاصة، كانت اللوحة تعرض **الإعداد العام** وتكتب فيه. القياس: قاعدة
  بـ`fontSize: 20`، سحب الشريط إلى `28`، اللوحة تقول «تم الحفظ» و«مفعّل حاليًا في هذا
  التبويب» — والصفحة تبقى عند `20`. صمتٌ تام لتغيير لم يحدث.
- الآن يتبع نطاقُ التحرير التبويبَ: تُعرض القيم الفعّالة، وتذهب الكتابة إلى القاعدة الحاكمة،
  ويسمّيها شريط التنبيه. وهو بالضبط ما تفعله اختصارات لوحة المفاتيح منذ 1.2 — فالسطحان
  صارا يقولان الشيء نفسه.
- «إعادة الضبط» في اللوحة تعيد ضبط النطاق الذي تحرّره، وتقول أيهما في نص التأكيد.
- ولا يُصدَّق `sender.tab.url` إلا إن كان `http`/`https`: صفحةُ إضافة مفتوحة في تبويب كانت
  تجعل العامل يجيب عن معرّف الإضافة بدل الموقع.

### كل تغيير إعداد كان يهدم الصفحة ويعيد بناءها

- كان `apply()` يزيل الصنف وورقة الأنماط وكل علامة ثم يعيد الفحص. القياس على صفحة بـ3000
  فقرة: عدد العناصر المنسَّقة **يهبط إلى صفر** ثم يعود خلال 564ms — أي الصفحة كلها ترتد إلى
  خطها الأصلي، أربع مرات في الثانية أثناء سحب الشريط.
- صار `commitSettings` يميّز: تغيّر `enabled`/`excluded`/`mode` يعني إعادة بناء، وأي شيء آخر
  يعني إعادة كتابة ورقة الأنماط، ثم `remeasureTypography()` عند تغيّر الحجم فقط.
- القياس نفسه بعد الإصلاح: العدد **لا يهبط أبدًا** (3006 → 3006) والاستقرار 373ms.
- تغيّر أنماط الصفحة صار يعيد التحقق من الأهلية بدل الهدم.

### تباعد الحروف كان يزيح ما لا يجب ولا يفعل ما يجب

- القياس: عرض المعاينة العربية **165.6px بالضبط** عند ‎−0.08 و0 و0.1 و0.2em — صفر أثر، لأن
  Blink لا يباعد النصوص المتصلة. وفي المقابل عند 0.2em كانت شارةٌ تتسع **35%** وخليةُ جدول
  **52%** في وضع التصميم.
- انتقلت `letter-spacing` من قاعدة `data-snfont-text` إلى `data-snfont-prose`، فلا تمسّ
  الواجهة. واللوحة تقول تحت الشريط إنه يؤثر في النص اللاتيني وحده.

### إصلاحات المحرك

- **عنوان المقال**: `header`/`footer` داخل `<main>` أو `<article>` صارت محتوًى لا واجهة. على
  ويكيبيديا كان `<h1>` داخل `header.mw-body-header` فيبقى بخط الموقع بينما يتغيّر كل ما تحته.
- **وحدة المقاس**: كُتب بالبكسل بدل `ch` مع `box-sizing: border-box`. كانت `ch` تُحسب لكل
  عنصر بخطه فتُنتج أربعة عروض على صفحة واحدة؛ العنوان كان يتجاوز الفقرات بـ94px من كل جهة.
- **Shadow DOM**: يمرّ الفحص الآن على العناصر أيضًا فيكتشف الجذور المفتوحة، ويتبنّى فيها
  ورقة أنماط مشتقّة من `buildCss()`، ويراقبها، ويحرّرها كاملة عند الإيقاف.
- **تغيّر مقاس النافذة**: مستمع `resize` مخنوق يعيد القياس. كانت نقطةُ توقّف متجاوبة تترك
  عنوانًا مثبّتًا على 60px بدل 40px إلى أجل غير مسمّى.

### النطاقات والشارة والاختصارات

- **التحقق من النطاقات**: `new URL()` ليس مُحقِّقًا. يرمّز Chromium المسافة داخل المضيف
  (`not a domain!!` → `not%20a%20domain!!`) ويقبلها، بينما يرفضها محلّل Node — فكانت حزمة
  الاختبارات تحرس محلّلًا غير الذي يُشحن. صار ناتج التطبيع يُختبر أمام نمط أسماء نطاقات
  حقيقي، مكرَّر في `content.js` ويحرسه `contract.test.mjs`.
- **الشارة لكل تبويب**: كانت تقول «مفعّل» على موقع مستثنى. صارت تتبع التبويب الذي طلب
  إعداداته عبر `sender.tab.id` — بلا أي إذن إضافي.
- **إعادة الحجم على قاعدة موقع** صارت تحذف مفتاحَي الحجم فتعود الوراثة، بدل تثبيت الموقع
  على 16px إلى الأبد.
- **اختصار عند حد المدى** يومض بالقيمة الحالية على الشارة بدل ألا يفعل شيئًا.

### الواجهة والتنظيف

- المعاينة في اللوحة تعرض السطرين العربي واللاتيني **بالحجم نفسه**؛ كان اللاتيني عند ثلثَي
  العربي فتخالف المعاينةُ كلَّ صفحة تعاينها.
- نص المساعدة تحت شريط الحجم في «إدارة المواقع» كان يقول «أقصى اليمين» والشريط `direction:
  ltr` فالحد الأدنى في أقصى اليسار.
- `use_dynamic_url` على ملفات الخطوط، فلا تستخرج الصفحةُ معرّفًا ثابتًا للإضافة من عناوينها.
  و`PRIVACY.md` يذكر صراحةً ما تستطيع الصفحة ملاحظته.
- حُذف كود ميّت: `article.html` معطّل بلا سكربت، وإحالة إلى `run-engine-checks.js` غير موجود،
  و`resetLabel` و`state.siteSettings` في اللوحة، وأربعة رموز تصميم، وقاعدة CSS مكرّرة،
  وتعليق مكرّر. و`logo-28.png` لم تعد تُشحن داخل الحزمة.
- حلقة تركيز لرابط التذييل في صفحة الإعدادات.

### الاختبارات

- من 56 إلى **72** اختبارًا: الشارة لكل تبويب، سلوك الاختصارات على قاعدة موقع، رفض مضيف
  صفحة الإضافة، نمط النطاقات المشترك، حصر التباعد في النثر، وحدة المقاس، التحديث في المكان،
  متابعة تغيّر المقاس، ترويسة المقال، جذور الظل، نطاق تحرير اللوحة، وتكافؤ المعاينة.
- ومجموعة Playwright تُحمّل الإضافة في Chromium حقيقي — 27 تحققًا للإصلاحات و24 للانحدار.
  التفاصيل في [docs/QA.md](docs/QA.md).

## 1.3.1 — 2026-08-22

متابعة لبلاغ: «تم الحفظ يظهر دائمًا، ولا يوجد زر حفظ التخصيص». كلا الأمرين كان صحيحًا،
ولسببين مختلفين تمامًا.

### زر الحفظ كان موجودًا لكنه خارج الشاشة
- شريط الحفظ كان آخر عنصر في المحرر تحت الشبكة كاملة، و`.settings-window` عليه
  `overflow: hidden` الذي **يُبطل `position: sticky`** لأي عنصر بداخله. النتيجة: الزر لا
  يظهر أبدًا على شاشة عادية، وهذا يُقرأ «لا يوجد زر».
- صار `overflow: clip` (يدوّر الحواف دون إنشاء حاوية تمرير)، والشريط `sticky` عند أسفل
  النافذة. مقيس: مرئي عند أعلى الصفحة ووسطها وأسفلها.
- الشريط نفسه يتلوّن عند وجود تغييرات غير محفوظة، فالحالة مرئية لا نصية فقط.

### «تم الحفظ» في اللوحة كان دائمًا
- `setSaveState('تم الحفظ')` كان يكتب النص ولا يعيده أبدًا، فتبقى اللوحة تعلن حفظًا حدث
  قبل دقائق. صار إشعارًا مؤقتًا: يظهر 1.8 ثانية بعد كتابة فعلية ثم يختفي، والحالة الهادئة
  فارغة تمامًا.

### الحفظ صار معاملة ذرّية
- كان `commitDraft` يعدّل الحالة **قبل** الكتابة، فلو فشلت الكتابة تبدو التغييرات محفوظة
  وهي ليست كذلك. صار يلتقط نسخة للتراجع ويستعيدها عند الفشل. مقيس بمحاكاة فشل التخزين:
  الحالة تبقى «تغييرات غير محفوظة» والزر مفعّلًا مع رسالة خطأ، والمحاولة التالية تنجح.
- خمسة اختبارات بنيوية جديدة تثبّت السلوك: من يكتب في التخزين، ومن يستدعي `persistSiteData`،
  وألّا يصل أي مؤقّت إلى التخزين، وألّا تكون الحالة الافتراضية رسالة نجاح، ووجود التراجع
  والالتصاق.

### شريط اللوحة السفلي
- كان نصّان طويلان يملآن العرض ويزاحمان نص الحالة. صارا إجراءين بأيقونات من نظام SnFont:
  «المواقع» بأيقونة `sliders-horizontal` مع نص قصير، وإعادة الضبط بأيقونة `rotate-ccw`
  وحدها مع tooltip واسم وصول كامل. عرض الإجراءات نزل من 197px إلى 117px بلا أي فيض.

## 1.3.0 — 2026-08-22

أُعيد فحص الإضافة على GitHub وYouTube وSubstack وويكيبيديا داخل متصفح حقيقي، فظهر أن
الإصدار السابق كان **لا يعمل عمليًا على الويب الحديث**. هذا الإصدار يصلح السبب الجذري.

### الخطوط لم تكن تُطبَّق أصلًا
- كان حارس «الخطوط المصمَّمة من الموقع» يسأل: هل سمّى الموقع خطًا؟ وكل موقع حديث يفعل.
  القياس على الصفحات الحقيقية: **GitHub 100% من 641 عنصرًا محميّ** (يعلن `Mona Sans VF`)،
  و**YouTube 98%** (يعلن `Roboto`). النتيجة: صفر تغيير على الصفحة.
- صار الحارس يحمي ما يجب حمايته فعلًا فقط: **خطوط الأيقونات** (تبديلها يحوّل الرموز إلى مربعات)
  و**الخطوط أحادية العرض**. القياس بعد الإصلاح: GitHub 98% معالَج والمتبقي `Mona Sans Mono`،
  YouTube 100%، Substack 100%.
- الحارس يفحص **الخط الأساسي فقط** لا القائمة كاملة؛ فحص القائمة كان يطابق احتياطيات الرموز
  مثل `Segoe UI Symbol` فيتخطى 96% من Substack.
- إيقاف SnFont على موقع بعينه يبقى متاحًا عبر مفتاح الموقع وقائمة الاستثناءات.

### تغيير النصوص دون كسر تخطيط المواقع
- فُصلت العلامة إلى اثنتين: `data-snfont-text` يغيّر **الخط فقط** ويُطبَّق في كل مكان آمن،
  و`data-snfont-prose` يضيف **الحجم وارتفاع السطر** ويُطبَّق على كتل النثر وحدها.
- الأزرار والتنقل والشارات تحتفظ بمقاساتها. قياسًا على GitHub: **صفر رابط أو زر** تغيّر حجمه،
  وعرض `main` و`header` مطابق تمامًا قبل/بعد (1265px).

### وضع القراءة كان يدمّر تخطيط التطبيقات
- كان `<main>` في GitHub يمرّ كـ«مقال» (10% نثر فقط، 36 حاوية grid) فيأخذ `max-width: 63ch`
  وينهار عمود المستودعات إلى حرف في كل سطر.
- أُضيف **حد نسبة النثر (45%)** لكل مرشّح: GitHub 10% فيُرفض، ويكيبيديا 79% فتُقبل.
- الأهم: صار مقاس القراءة يُطبَّق على **كتل النثر** لا على الحاوية. تضييق حاوية قد يُسقط شبكة
  تملكها؛ تضييق فقرة لا يمكنه ذلك. تحقُّق على ويكيبيديا: `max-width` الجذر = `none` وعرضه 957px
  دون تغيير، بينما الفقرات عند 792px.
- رُفع حظر `display: grid` عن الجذر لأنه كان يرفض مقالات حقيقية (ويكيبيديا).

### حجم الـPopup كان يتمدد وينكمش
- أبناء `.app` عناصر grid بـ`min-width: auto` فلا تنكمش تحت محتواها؛ كان التذييل يفيض إلى
  363px بينما اللوحة 358px، والرقم **يتغير مع نص الحالة** فيتبع Chromium ذلك التذبذب.
- أُضيف `grid-template-columns: minmax(0, 1fr)` و`min-width: 0` للأبناء. القياس بعد الإصلاح:
  **358/380 ثابتة عبر تسع حالات** (المظاهر الثلاثة، وضع القراءة، الإعدادات الدقيقة، الاستثناء،
  إلغاؤه، الإيقاف العام).
- `.sn-sr-only` صار مثبّتًا عند بداية السطر بدل موضعه الساكن.

### إدارة المواقع: حفظ صريح
- المحرر صار **معاينة مؤقتة**: أي تعديل يبقى محليًا حتى تضغط **«حفظ التخصيص»**.
- أُزيل آخر أثر للحفظ التلقائي: كان `persistSiteData` يكتب «تُحفظ التغييرات تلقائيًا» بعد كل
  عملية، فتبدو الصفحة وكأنها تحفظ من تلقاء نفسها. صار مؤشر الحالة صريحًا:
  «لا توجد تغييرات غير محفوظة» ← «تغييرات غير محفوظة» ← «جارٍ الحفظ…» ← **«تم الحفظ»** لثانيتين
  بعد كتابة فعلية فقط، ثم يعود. لا مؤقتات ولا debounce ولا كتابة دون ضغط زر.
- شعار الصفحة صار مطابقًا لبناء شعار اللوحة (نفس نصف القطر ونسبة مؤشر الكهرمان).
- تذييل الصفحة: «تطوير وتصميم: سلطان» ورابط صفحة المشروع.
- زر «تجاهل التغييرات»، ومؤشر «معاينة — لم تُحفظ بعد»، وتأكيد عند مغادرة الصفحة أو تبديل موقع
  بتغييرات غير محفوظة.
- لم تعد كتابة سطح آخر للتخزين تمسح ما تحرّره الآن.

### الواجهة
- **عنصر مظهر مقسّم** في الترويسة (نهاري · يتبع النظام · ليلي) بأيقونات موحّدة، بدل زر واحد
  يدور بين ثلاث حالات ولا يُظهر الخيارات.
- **أُزيل مبدّل النطاق** من اللوحة. اللوحة تحرّر الإعداد العام فقط، وتخصيص المواقع صار في
  «إدارة المواقع» وحدها كما ينبغي لخيار يُحفظ بقرار.
- ضغط المقاسات لتقترب من تطبيقات macOS: الشعار 26px، الاسم 18px، الأوضاع 32px، صفوف
  التحكم 44px، مع بقاء أرضية 11px لنص الواجهة.

### إصلاحات أخرى
- `loadSettings` كان يغلّف `apply()` داخل `try` نفسه، فأي خطأ في المحرك يُفهم على أنه Service
  Worker نائم ويُستبدَل بإعدادات افتراضية بصمت. صار الخطأ يظهر.
- حُذف الماسح الميت لقواعد CSS (`collectAuthoredFontSelectors`) الذي كان يمشي على كل ورقة
  أنماط في كل تطبيق.

## 1.2.0 — 2026-08-22

### محرك الصفحة
- إصلاح ارتداد كامل للصفحة عند أي تغيير `class` على `<html>` أو `<body>`: كان تبديل صنف
  التمرير أو الوضع الداكن في الموقع يمسح كل العلامات ويعيد فحص الصفحة، فيومض النص بين
  حجمه الأصلي والمحسّن. صار الإبطال مؤجَّلًا ومجمَّعًا، ولا يُبطَل شيء إلا إذا تغيّرت
  عائلة الخط الفعلية لجذر المستند.
- تجميع رشقات الكتابة في التخزين: سحب شريط الحجم كان يسبب إعادة تطبيق كاملة لكل تبويب
  مفتوح مع كل كتابة. صار هناك تأخير 250ms ورمز جيل يمنع وصول استجابة قديمة بعد الحديثة.
- ميزانية زمنية للفحص (8ms لكل شريحة) بدل عدّ 8000 عقدة، فلا يتوقف التمرير على الصفحات الضخمة.
- توسيع نطاق المحارف اللاتينية ليشمل علامات الترقيم والعملات والرموز الموجودة فعلًا في
  الخط المرفق؛ كانت علامات التنصيص والشرطة الطويلة والقطع تسقط خارج العائلة وتُرسم بخط مختلف.
- استعادة ذاتية لحجم العنصر عندما تعيد مكتبة حركة كتابة سمة `style` كاملة عليه.
- خفض تكلفة حارس الخطوط المصممة بترتيب الفحص الرخيص أولًا.
- تحديد وتيرة إعادة البحث عن المقال على صفحات التطبيقات التي لا تحتوي مقالًا أصلًا.
- إبلاغ الواجهة بحالة المحرك الحقيقية عبر `SNFONT_GET_PAGE_STATE`.

### الواجهة
- **اللوحة تقول الحقيقة الآن**: شريط حالة يوضّح سبب عدم حدوث شيء — صفحة محمية، أو تحتاج
  إعادة تحميل، أو مستثناة، أو متوقفة، أو وضع قراءة بلا مقال موثوق. كان وضع القراءة يعمل
  بلا أثر على صفحات بلا مقال بينما تعلن اللوحة أنه «مفعّل حاليًا».
- إذن `activeTab` للتمييز بين صفحة Brave محمية وصفحة عادية لم يُحقن فيها السكربت بعد؛
  كانت كل صفحة غير محقونة تُعرض على أنها «صفحة محمية».
- مظهر **يتبع النظام** أصبح الافتراضي، مع دورة نظام/نهاري/ليلي، وزر مظهر مستقل في الترويسة
  بدل زر ثالث داخل صف الأوضاع كان يبدو وضعًا للصفحة.
- إعادة الضغط على الوضع النشط لم تعد تمسح ست قيم ضبطها المستخدم.
- الترويسة صارت RTL كبقية اللوحة، ومسار مفتاح التبديل صار معكوسًا مع اتجاه القراءة.
- انضباط قارئ الشاشة: منطقة إعلان واحدة للنتائج النهائية بدل خمس مناطق تُطلق مع كل حركة شريط.

### صفحة إدارة المواقع
- إصلاح فقدان بيانات: كان المُطبِّع المحلي يرفض `localhost` وعناوين IP والنطاقات أحادية
  الاسم التي يخزّنها باقي الإضافة، فيحذفها عند أول حفظ. صار يستخدم عقد النطاقات المشترك.
- إزالة «التخصيص الشبح»: كان كل موقع يُكتب له `enabled: true` حتى بلا تخصيص، فيحجب أي قاعدة
  نطاق أعلى منه ولا يمكن مسحه أبدًا.
- المواقع المستثناة فقط أصبحت قابلة للفتح في المحرر.
- تأكيد قبل الاستيراد مع بيان ما سيُستبدل، وملف بلا `settings` لم يعد يصفّر الإعداد العام.
- سحب شريط الحجم لم يعد يعيد بناء قائمة المواقع داخل منطقة إعلان حيّة.
- زر مظهر مستقل، ونقل التركيز بعد الحذف، وإصلاح تسلسل العناوين (كان `<h1>` بحجم 10px).

### الإعدادات والاختصارات
- اختصارات تكبير/تصغير الخط تتحرك الآن بخطوة 2px داخل المدى 10–32 بدل القفز بين خمسة
  مستويات فقط؛ وكانت لا تفعل شيئًا لأي حجم يدوي خارجها.
- الاختصارات تحرّر قاعدة الموقع الحاكمة للتبويب الحالي إن وُجدت، بدل كتابة عامة صامتة لا أثر لها.
- تحرير موقع فرعي لم يعد يُسقِط قاعدة النطاق الأب: تبدأ القاعدة الجديدة نسخةً مما تحجبه.
- تحديث شارة الإضافة عند استيقاظ Service Worker وليس عند التثبيت والإقلاع فقط.
- حذف معالجَي رسائل ميّتين (`SNFONT_APPLY_PRESET` و`SNFONT_REFRESH_SETTINGS`).

### نظام التصميم
- طبقة رموز واحدة في `src/shared/tokens.css` تحل محل نظامي تصميم منفصلين كانا يعرّفان
  70 خاصية مخصصة، ويكرران 12 لونًا بأسماء مختلفة، ويختلفان في ثمانية أدوار.
- أرضية 11px لنص الواجهة: كانت عناصر أساسية تُعرض بالعربية بحجم 8–9px.
- `--amber-ink` للنص الكهرماني على الأسطح الفاتحة (كان 2.08:1، أي غير مقروء عمليًا).
- مجموعة أيقونات واحدة على شبكة 24 في `src/shared/icons.js` بـ `currentColor` بدل
  فلاتر `brightness/invert` وثمانية ملفات SVG بألوان مثبّتة.
- حالات تفاعل كاملة (hover/active/focus/disabled) للعناصر التي كانت بلا أي رجع بصري.
- مخطط تخزين 3 مع ترحيل المظهر إلى «يتبع النظام» لمن لم يختر مظهرًا صراحة.

## 1.1.0 — 2026-08-22

- فصل المظهر الليلي والنهاري عن أوضاع المواقع؛ أصبح خاصًا بواجهة SnFont فقط.
- ترحيل الإعداد القديم `night` تلقائيًا إلى وضع القراءة الآمن دون تغيير ألوان الصفحة.
- إضافة محرك **Safe Prose Island** الذي يعالج المقال الموثوق فقط ولا يستخدم `body` جذرًا احتياطيًا.
- حماية تخطيطات التطبيقات والشبكات والأشرطة الجانبية والعناصر التفاعلية من التضييق أو إعادة التنسيق.
- حماية الخطوط المصممة من الموقع في وضعي التصميم والقراءة، مع مراقبة المحتوى والأنماط الديناميكية.
- حصر إعادة اكتشاف المقال في المرشحين الدلاليين لتقليل العمل على التطبيقات كثيرة التحديث.
- إعادة معايرة Popup على قياسات Figma، وصقل الشعار والـRTL وحالتي الضوء والظلام.
- تكييف ارتفاع Popup مع حد Chromium البالغ 600px مع إبقاء التمرير داخل منطقة المحتوى فقط.
- اعتماد Cairo لعناوين الأداة وعناصرها الوظيفية وAlmarai للنصوص والوصف.
- إضافة مخطط تخزين 2 واختبارات ترحيل وفصل لمظهر الواجهة.
- رفع الحد الأدنى إلى Chromium 111 لضمان دعم خصائص الواجهة الحديثة المستخدمة في التصميم.

## 1.0.0 — 2026-08-22

- أول إصدار من محرك الخطوط العربي والإنجليزي الذكي.
- أوضاع التصميم والقراءة (كان المظهر الليلي مرتبطًا بالصفحة في هذا الإصدار).
- تحكم بالحجم وارتفاع السطر وتباعد الحروف وعرض النص.
- إعدادات واستثناءات مستقلة لكل موقع.
- مراقبة محتوى الصفحات الديناميكية على دفعات محدودة.
- Popup مطابق لهوية SnFont وصفحة متقدمة لإدارة المواقع.
- اختصارات لوحة مفاتيح ودعم RTL/LTR وخطوط محلية بالكامل.

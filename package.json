{
  "name": "snfont-brave-extension",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "description": "SnFont smart Arabic and English reading assistant for Brave and Chromium browsers.",
  "scripts": {
    "check": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "package": "bash scripts/package-extension.sh",
    "harness": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
